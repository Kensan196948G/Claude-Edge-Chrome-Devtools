================================================================================
  Claude-EdgeChromeDevTools 使用方法
  Usage Guide
================================================================================

【基本的な使い方】

■方法1: start.bat（対話型ランチャー）の使用

  1. start.bat をダブルクリック

  2. メニューから機能を選択:

     ===============================================
      PowerShell Script Launcher Menu
     ===============================================

      [Claude DevTools Main]
      1. Claude Edge DevTools Setup
      2. Claude Chrome DevTools Setup

      [Test and Utility]
      3. Edge DevTools Connection Test
      4. Chrome DevTools Connection Test

      [Windows Terminal Settings]
      5. Windows Terminal Setup Guide
      6. Auto-Configure Windows Terminal (PowerShell)

      0. Exit
     ===============================================

  3. 番号を入力してEnter

■方法2: PowerShellスクリプトの直接実行

  1. PowerShellを開く（Windows Terminal推奨）

  2. プロジェクトディレクトリに移動:
     cd D:\Claude-EdgeChromeDevTools

  3. スクリプトを実行:

     # Edge版
     powershell -ExecutionPolicy Bypass -File scripts\main\Claude-EdgeDevTools.ps1

     # Chrome版
     powershell -ExecutionPolicy Bypass -File scripts\main\Claude-ChromeDevTools-Final.ps1

【詳細な実行フロー】

■ステップ1: ブラウザ選択（Claude-EdgeDevTools.ps1の場合）

  🌐 ブラウザを選択してください:
  [1] Microsoft Edge
  [2] Google Chrome
  番号を入力 (1-2, デフォルト: 1):

  ※ Claude-ChromeDevTools-Final.ps1の場合はChromeがデフォルト

■ステップ2: ポート自動選択

  ✅ 自動選択されたポート: 9222

  ※ config.jsonのports配列から利用可能なポートを自動選択

■ステップ3: プロジェクト選択

  📦 プロジェクトを選択してください

  [1] project-a
  [2] project-b
  [3] project-c

  番号を入力: 2

  ✅ 選択プロジェクト: project-b

■ステップ4: ブラウザDevTools起動

  🌐 Microsoft Edge DevTools 起動準備...
  📁 プロファイルディレクトリ作成: C:\DevTools-edge-9222

  ✅ DevTools設定を適用:
     - Disable cache (while DevTools is open)
     - Auto-open DevTools for popups
     - Preserve log
     - Show timestamps

  🌐 起動URL: http://localhost:9222
  ⏳ Microsoft Edge 起動待機中...

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ Microsoft Edge DevTools 接続テスト成功!
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

■ステップ5: run-claude.sh生成

  ✅ run-claude.sh 生成完了

  ※ プロジェクトルート（Z:\project-b\run-claude.sh）に生成

■ステップ6: Statusline設定（有効な場合）

  🎨 Claude Code Statusline 設定を生成中...
  ✅ Statusline 設定完了: /mnt/LinuxHDD/project-b/.claude/settings.json

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    Statusline設定を反映させるには
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  【方法1: すぐに反映（推奨）】
     Claude Codeで以下のコマンドを実行:
     /statusline

  【方法2: 確実に反映】
     1. exit でClaude Codeを終了
     2. 再度スクリプトを実行

■ステップ7: SSH接続 + Claude Code起動

  🎉 セットアップ完了

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🚀 Claudeを起動します...
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  🔍 DevTools 応答確認...
  ✅ DevTools 接続成功!

  🚀 Claude 起動 (port=9222)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  📝 初期プロンプトを自動入力します...

  （Claude Codeセッション開始）

【テストスクリプトの使用】

■Edge DevTools接続テスト

  scripts\test\test-edge.ps1 を実行:

  - ポート 9222-9225 の順で接続テスト
  - 成功したポートの情報を表示
  - スクリーンショット撮影（オプション）

■Chrome DevTools接続テスト

  scripts\test\test-chrome.ps1 を実行:

  - Edge版と同様の機能

【Claude Codeセッション中の操作】

■Statuslineの表示内容

  📁 /mnt/LinuxHDD/project-b  🌿 main  🤖 sonnet-4.5  📟 v0.8.0  🎨 auto
  🧠 [████████░░] 80%

  - 📁 カレントディレクトリ
  - 🌿 Gitブランチ
  - 🤖 モデル名
  - 📟 Claudeバージョン
  - 🎨 出力スタイル
  - 🧠 コンテキスト使用率

■Statusline更新コマンド

  Claude Code内で実行:
  /statusline

■セッション終了

  - Ctrl+C または exit で終了
  - 自動再起動が設定されている場合は3秒後に再起動

【高度な使い方】

■複数プロジェクトの同時実行

  1. 異なるポートを使用することで、複数プロジェクトを同時に起動可能
  2. config.jsonのports配列に十分なポート数を設定
  3. 各プロジェクトごとにスクリプトを実行

■カスタム初期プロンプト

  scripts\main\Claude-EdgeDevTools.ps1（または Claude-ChromeDevTools-Final.ps1）
  の INIT_PROMPT変数を編集（heredoc形式）

  現在の形式:
    INIT_PROMPT=$(cat << 'INITPROMPTEOF'
    プロンプト内容をここに記述
    INITPROMPTEOF
    )

  ※ シングルクォート付きデリミタにより、バッククォートや
     二重引用符を含むテキストも安全に記述可能

■Statuslineのカスタマイズ

  scripts\statusline.sh を編集して独自の表示を追加可能

【次のステップ】
  - 04_設定ファイル詳細(Configuration-Details).txt で設定のカスタマイズ方法を学習
  - 05_トラブルシューティング(Troubleshooting).txt で問題解決方法を確認
