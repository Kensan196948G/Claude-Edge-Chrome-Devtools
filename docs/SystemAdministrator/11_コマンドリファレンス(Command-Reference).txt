================================================================================
  Claude-EdgeChromeDevTools コマンドリファレンス
  Command Reference
================================================================================

【PowerShellコマンド】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ メインスクリプト実行
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ Claude-EdgeDevTools.ps1

【構文】
  powershell -ExecutionPolicy Bypass -File scripts\main\Claude-EdgeDevTools.ps1

【または】
  pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\main\Claude-EdgeDevTools.ps1

【説明】
  Microsoft Edgeをデフォルトブラウザとして使用し、Claude Code開発環境を
  セットアップします。

【対話型プロンプト】
  1. ブラウザ選択: Edge (1) または Chrome (2)
  2. プロジェクト選択: 数字入力

【処理内容】
  - config.json読み込み
  - ポート自動選択
  - ブラウザDevTools起動
  - run-claude.sh生成
  - Statusline設定
  - SSH接続 + Claude起動

【例】
  # Windows Terminal から実行
  cd D:\Claude-EdgeChromeDevTools
  pwsh -NoProfile -ExecutionPolicy Bypass -File scripts\main\Claude-EdgeDevTools.ps1

─────────────────────────────────────────────────────────────

▶ Claude-ChromeDevTools-Final.ps1

【構文】
  powershell -ExecutionPolicy Bypass -File scripts\main\Claude-ChromeDevTools-Final.ps1

【説明】
  Google Chromeをデフォルトブラウザとして使用し、Claude Code開発環境を
  セットアップします。

【対話型プロンプト】
  1. ブラウザ選択: Edge (1) または Chrome (2、デフォルト)
  2. プロジェクト選択: 数字入力

【Edge版との相違点】
  - デフォルトブラウザがChrome
  - DevTools Preferences事前設定なし
  - ログ出力に追加情報（PORT表示、MCP確認）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ テストスクリプト
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ test-edge.ps1

【構文】
  powershell -ExecutionPolicy Bypass -File scripts\test\test-edge.ps1

【説明】
  Microsoft Edge DevTools接続をテストします。

【処理内容】
  - ポート 9222-9225 を順番にテスト
  - /json/version エンドポイントに接続試行
  - バージョン情報表示
  - WebSocketデバッガーURL表示

【出力例】
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ Edge DevTools 接続テスト成功!
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  📊 接続情報:
     - ポート: 9222
     - Browser: Chrome/120.0.6099.109
     - Protocol-Version: 1.3
     - V8-Version: 12.0.267.8

─────────────────────────────────────────────────────────────

▶ test-chrome.ps1

【構文】
  powershell -ExecutionPolicy Bypass -File scripts\test\test-chrome.ps1

【説明】
  Google Chrome DevTools接続をテストします。機能はtest-edge.ps1と同等です。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ セットアップスクリプト
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ setup-windows-terminal.ps1

【構文】
  powershell -ExecutionPolicy Bypass -File scripts\setup\setup-windows-terminal.ps1

【説明】
  Windows Terminal用のClaude DevToolsプロファイルを自動生成します。

【生成される設定】
  - プロファイル名: Claude DevTools
  - フォント: Cascadia Code (Size 18)
  - カラーテーマ: One Half Light (Bright)
  - 背景透明度: 95%
  - カーソル: バー（白）

【設定ファイルパス】
  %LOCALAPPDATA%\Packages\Microsoft.WindowsTerminal_...\LocalState\settings.json

【例】
  pwsh -ExecutionPolicy Bypass -File scripts\setup\setup-windows-terminal.ps1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ランチャー
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ start.bat

【構文】
  start.bat

【または】
  ダブルクリック

【説明】
  対話型メニューランチャーを起動します。

【メニュー項目】
  [1] Claude Edge DevTools Setup
  [2] Claude Chrome DevTools Setup
  [3] Edge DevTools Connection Test
  [4] Chrome DevTools Connection Test
  [5] Windows Terminal Setup Guide
  [6] Auto-Configure Windows Terminal (PowerShell)
  [0] Exit

【動作】
  - 管理者権限 + Windows Terminal未起動の場合、自動的にWindows Terminalで再起動
  - 選択されたスクリプトを pwsh で実行
  - メインスクリプト（1,2）は実行後メニューに自動復帰

【例】
  # エクスプローラーから
  start.bat をダブルクリック

  # コマンドプロンプトから
  cd D:\Claude-EdgeChromeDevTools
  start.bat

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【Bashコマンド（Linux側）】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ run-claude.sh（動的生成）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【パス】
  <プロジェクトルート>/run-claude.sh
  例: /mnt/LinuxHDD/my-project/run-claude.sh

【構文】
  ./run-claude.sh

【説明】
  Claude Codeをリモートデバッグモードで起動します。
  このスクリプトはPowerShellメインスクリプトによって自動生成されます。

【処理フロー】
  1. DevTools応答確認（最大10回リトライ、2秒間隔）
  2. 環境変数設定
     - CLAUDE_CHROME_DEBUG_PORT
     - MCP_CHROME_DEBUG_PORT
     - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS
  3. 初期プロンプト自動入力（heredoc形式で定義）
  4. claude --dangerously-skip-permissions 起動
  5. 異常終了時の自動再起動（3秒遅延）

【環境変数】
  PORT              : DevToolsポート番号（動的設定）
  RESTART_DELAY     : 再起動までの遅延秒数（デフォルト: 3）
  INIT_PROMPT       : 初期プロンプト文字列（heredoc形式）
  CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS : Agent Teams有効化 (1)

【INIT_PROMPT定義方式】
  heredoc方式（bash double-quoted stringは使用不可）:
  INIT_PROMPT=$(cat << 'INITPROMPTEOF'
  プロンプト内容
  INITPROMPTEOF
  )

【シグナルハンドリング】
  Ctrl+C (SIGINT)   : 正常終了

【例】
  # SSH接続後
  cd /mnt/LinuxHDD/my-project
  ./run-claude.sh

─────────────────────────────────────────────────────────────

▶ statusline.sh

【パス】
  ~/.claude/statusline.sh
  または
  <プロジェクトルート>/.claude/statusline.sh

【構文】
  ./statusline.sh

【説明】
  Claude Code Statusline表示をカスタマイズします。

【表示項目】
  📁 カレントディレクトリ
  🌿 Gitブランチ
  🤖 モデル名
  📟 Claudeバージョン
  🎨 出力スタイル
  🧠 コンテキスト使用率（プログレスバー）

【依存】
  - jq (JSON処理)
  - git (ブランチ情報取得)
  - claude --version (バージョン情報)

【設定ファイル】
  ~/.claude/settings.json
  {
    "statusLine": {
      "type": "command",
      "command": "~/.claude/statusline.sh",
      "padding": 0
    }
  }

【手動実行】
  ./statusline.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【Claude Codeコマンド（セッション内）】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ Claude Code内部コマンド
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ /statusline

【説明】
  Statusline設定を即座に再読み込みして反映します。

【使用タイミング】
  - statusline.sh を更新した後
  - settings.json を変更した後
  - Statusline表示が正しくない場合

【例】
  Claude Code内で:
  /statusline

─────────────────────────────────────────────────────────────

▶ /help

【説明】
  Claude Codeのヘルプを表示します。

─────────────────────────────────────────────────────────────

▶ exit

【説明】
  Claude Codeセッションを終了します。

【例】
  Claude Code内で:
  exit

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【SSHコマンド】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ SSH接続（PowerShellから自動実行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ ssh（リモートポートフォワーディング付き）

【構文】
  ssh -t -o ControlMaster=no -o ControlPath=none `
      -R "${PORT}:127.0.0.1:${PORT}" $LinuxHost `
      "cd $LinuxBase/$ProjectName && ./run-claude.sh"

【オプション説明】
  -t                    : 疑似端末割り当て（対話的セッション）
  -o ControlMaster=no   : 接続多重化を無効化
  -o ControlPath=none   : 制御パスを使用しない
  -R PORT:127.0.0.1:PORT: リモートポートフォワーディング
                          Linux側のPORTをWindows側のlocalhost:PORTに転送

【例】
  ssh -t -o ControlMaster=no -o ControlPath=none \
      -R "9222:127.0.0.1:9222" windows-dev \
      "cd /mnt/LinuxHDD/my-project && ./run-claude.sh"

─────────────────────────────────────────────────────────────

▶ ssh（通常接続）

【構文】
  ssh windows-dev

【説明】
  リモートLinuxホストに通常のSSH接続を行います。

【前提条件】
  ~/.ssh/config に以下の設定が必要:
  Host windows-dev
    HostName 192.168.1.100
    User your-username
    IdentityFile ~/.ssh/id_rsa

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【ユーティリティコマンド】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ポート確認
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ Get-NetTCPConnection（PowerShell）

【構文】
  Get-NetTCPConnection -LocalPort 9222 -State Listen

【説明】
  指定ポートがリスニング状態か確認します。

【例】
  # ポート9222の確認
  Get-NetTCPConnection -LocalPort 9222 -State Listen

  # 複数ポートの確認
  Get-NetTCPConnection -LocalPort 9222,9223,9224,9225 -State Listen

─────────────────────────────────────────────────────────────

▶ netstat（Windows）

【構文】
  netstat -ano | findstr :9222

【説明】
  ポート9222を使用しているプロセスを特定します。

【例】
  # ポート9222を使用しているプロセスID確認
  netstat -ano | findstr :9222

  # プロセス終了
  taskkill /PID <プロセスID> /F

─────────────────────────────────────────────────────────────

▶ fuser（Linux）

【構文】
  fuser -k 9222/tcp

【説明】
  ポート9222を使用しているプロセスを強制終了します。

【例】
  # ポート9222のプロセスを確認
  fuser 9222/tcp

  # ポート9222のプロセスを強制終了
  fuser -k 9222/tcp

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ DevTools確認
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶ curl（Linux）

【構文】
  curl -s http://127.0.0.1:9222/json/version

【説明】
  DevToolsエンドポイントからバージョン情報を取得します。

【例】
  # バージョン情報取得
  curl -s http://127.0.0.1:9222/json/version

  # 整形表示
  curl -s http://127.0.0.1:9222/json/version | jq .

  # 接続確認のみ
  curl -sf --connect-timeout 2 http://127.0.0.1:9222/json/version >/dev/null 2>&1

─────────────────────────────────────────────────────────────

▶ Invoke-WebRequest（PowerShell）

【構文】
  Invoke-WebRequest -Uri "http://localhost:9222/json/version" -UseBasicParsing

【説明】
  DevToolsエンドポイントに接続テストを行います。

【例】
  # 接続テスト
  Invoke-WebRequest -Uri "http://localhost:9222/json/version" `
                    -UseBasicParsing -TimeoutSec 3

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【ブラウザ起動コマンド】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ Microsoft Edge（手動起動）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【構文】
  "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" `
    --remote-debugging-port=9222 `
    --user-data-dir="C:\DevTools-edge-9222" `
    --no-first-run `
    --no-default-browser-check `
    --remote-allow-origins=* `
    --auto-open-devtools-for-tabs `
    http://localhost:9222

【オプション説明】
  --remote-debugging-port=9222      : リモートデバッグポート指定
  --user-data-dir="..."             : 専用プロファイルディレクトリ
  --no-first-run                    : 初回起動画面をスキップ
  --no-default-browser-check        : デフォルトブラウザ確認をスキップ
  --remote-allow-origins=*          : すべてのオリジンを許可
  --auto-open-devtools-for-tabs     : タブのDevTools自動起動

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ Google Chrome（手動起動）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【構文】
  "C:\Program Files\Google\Chrome\Application\chrome.exe" `
    --remote-debugging-port=9222 `
    --user-data-dir="C:\DevTools-chrome-9222" `
    --no-first-run `
    --no-default-browser-check `
    --remote-allow-origins=* `
    http://localhost:9222

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【参考リンク】

- PowerShell公式ドキュメント: https://docs.microsoft.com/powershell/
- Chrome DevTools Protocol: https://chromedevtools.github.io/devtools-protocol/
- SSH公式マニュアル: https://www.openssh.com/manual.html
- Claude Code: https://claude.ai/code
